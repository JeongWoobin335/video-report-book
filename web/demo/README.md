미리 만들어 둔 예시 결과. 화면에서 `#demo`로 연다 — 서버도 AI 한도도 쓰지 않는다.
가상 강의 샘플(`samples/lecture-http-caching.mp4`)을 실제 서비스와 같은 엔진으로 돌린 결과 그대로다.
예시에 한해 퀴즈 정답(`quiz.json`)을 함께 두고 브라우저에서 채점한다. 실제 작업의 정답은 서버 밖으로 나가지 않는다.
