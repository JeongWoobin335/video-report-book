# video-report-book — 스킬 묶음

영상(회의·교육 등)을 올리면 → 음성 전사 + 화면 읽기 → 통합 리포트 → 이해도 퀴즈 + 요약 만화.

## 스킬이란 (이 프로젝트에서)

스킬은 Claude가 그 일을 잘하도록 건네는 지식 묶음이다.

```
<skill-name>/
├── SKILL.md      언제 쓰는지 + 작업 흐름 + 왜 그렇게 하는지 (짧게)
├── references/   필요할 때만 읽는 지식 — 형식 가이드, 작성법, 좋은 예/나쁜 예
├── assets/       결과물에 그대로 쓰이는 파일 — HTML 템플릿, CSS, SVG
├── scripts/      매번 똑같이 돌아야 하는 부분만 — 추출, 병합, 검증, 렌더링, 채점
└── evals/        테스트 프롬프트와 샘플 입력
```

판단과 글쓰기는 Claude가 하고, 스크립트는 결정적인 일만 한다.
웹 서비스의 워커는 Claude Agent SDK에 이 스킬 묶음을 로드해서 돌린다 — 스킬을 고치면 서비스가 그대로 좋아진다.

## 8개 구성

| 스킬 | 하는 일 | 주된 내용물 |
|---|---|---|
| `video-ingest` | 영상 → 오디오 + 키프레임 | scripts (ffmpeg) |
| `speech-transcribe` | 오디오 → 시간 붙은 전사 | scripts + 엔진 설정·한국어 팁 |
| `screen-read` | 키프레임 → 화면 글자와 내용 | 화면 유형별 읽는 법 |
| `integrated-report` | 전사 + 화면 → 통합 리포트 | 유형별 리포트 형식, 근거 규칙, HTML 템플릿 |
| `comprehension-quiz` | 리포트 → 이해도 퀴즈 | 문항 작성법(교육용/회의용), 퀴즈 UI 템플릿 |
| `comic-recap` | 리포트 → 요약 만화 | 스토리보드·연출 가이드, 말풍선 레이아웃 |
| `quiz-grading` | 응답 → 점수 + 다시 볼 구간 | scripts |
| `video-report-pipeline` | 위를 순서대로 | 흐름만 |

## 작업 폴더

한 영상 = 한 폴더. 스킬들은 이 폴더의 파일로만 이어진다.

```
work/<이름>/
  video.mp4
  audio.wav            video-ingest
  frames/0001.jpg ...  video-ingest
  transcript.json      speech-transcribe
  screens.json         screen-read
  timeline.md          integrated-report (전사+화면을 시간순으로 합친 읽기용 문서)
  report.md            integrated-report (원본) → report.html, report.json (파생)
  quiz.json            comprehension-quiz
  comic/               comic-recap
  attempts/            quiz-grading
```

근거는 **타임스탬프**다. 리포트의 모든 항목은 `[03:02]`를 달고, 퀴즈와 만화는 리포트 항목에서 나오므로 같은 타임스탬프를 물려받는다. 그래서 오답에 "03:02부터 다시 보세요"가 가능하다.

## 진행 순서

한 스킬씩: 초안 → 테스트 프롬프트 실행 → 결과 검토 → 개선.
테스트 작업공간(`skills/<name>-workspace/`)은 스킬이 마무리되면 지운다. 남기는 것은 스킬 폴더의 `evals/`(테스트 프롬프트와 샘플 입력)와 `docs/examples/`(결과물 예시)뿐.

1. `integrated-report` ✔ 2026-09-17 — 2회 반복. 가상 입력 3종(회의, 강의, 음성만)에서 채점 기준 27/27, 스킬 없이 돌린 기준선은 20/27. 예시: `docs/examples/`. 미검증: 실제(지저분한) 전사, 1시간 넘는 영상, general 유형.
2. `comprehension-quiz` ✔ 2026-09-17 — 1회. 강의 10문항·회의 8문항, 채점 기준 22/22 (기준선 10/22). 난이도 대신 `기억`/`이해` 구분, 회의용은 점수 대신 "다시 확인하실 부분". 퀴즈 화면 동작을 브라우저에서 확인함. 미검증: general 유형, 긴 리포트.
   이후 스킬부터는 스킬 없는 기준선은 돌리지 않는다 — 스킬 있는 버전만 돌려 결과물 품질을 직접 본다.
3. `quiz-grading` ✔ 2026-09-17 — 1회. 가상 응답(강의 7명, 회의 5명)에 심어 둔 패턴(공유된 오해, 찍은 흔적, 문항 문제, 이해≪기억, 개인 부진)을 모두 짚음. 회의용은 개인 점수표 없이 "~로 알고 있는 분이 N명" + 공지 권고. 결과 페이지 렌더링을 브라우저에서 확인함. 미검증: 응답 수십 명 규모, 무응답이 섞인 경우.
4. `comic-recap` ✔ 2026-09-17 — 이미지 생성 없이 시작: SVG 캐릭터 에셋(`assets/cast.json`, `characters.js`) + 영상의 실제 화면을 컷 안 보드에 띄움. 강의·회의 각 8컷, 렌더링 확인. 그림체는 추후 재검토(에셋 교체 또는 AI 이미지 옵션 — `comic.json` 구조는 그대로 입력이 됨).
5. `video-ingest`, `speech-transcribe`, `screen-read` ✔ 2026-09-17 — 직접 만든 테스트 영상(`samples/lecture-http-caching.mp4`, 슬라이드 8장 + 한국어 TTS 4.3분)에서 키프레임 8장 정확히 추출, GPU 전사 1분 이내. 화면 읽기는 API 없이 Claude가 키프레임을 직접 봄. 화자 분리는 하지 않음.
6. `video-report-pipeline` ✔ 2026-09-17 — 순서표 + `status.py`(파일 존재로 진행 상태 판단, 오래된 결과물 표시) + `index.html`.
7. **전체 통합 테스트 ✔ 2026-09-17** — 에이전트 하나가 `samples/lecture-http-caching.mp4`만 받아 파이프라인 스킬을 따라 끝까지 진행. 약 4분 30초에 리포트(33항목)·퀴즈(10문항)·만화(8컷)·모아보기 페이지 생성, 실패한 단계 없음. 실제 음성 인식 결과("이 태그", "맥스 에이지")를 화면 표기로 바로잡았고, 강사 말실수(3600초를 "하루")는 리포트에 둘 다 기록 → 퀴즈·만화에서 제외. 브라우저에서 확인: 리포트의 타임스탬프 클릭 시 영상 이동, 만화에 실제 슬라이드 배경. 마찰 기록에서 나온 수정(전사 용어 재실행 기준, 어긋난 내용의 하류 차단 강화, 근거 대조표 중복 제거, 문항 수·기억/이해 기준 등)을 반영함.
   결과 확인: `python tools/serve.py` 후 http://127.0.0.1:8765/work/lecture-http-caching/index.html (영상 구간 이동은 Range를 지원하는 서버에서만 동작 — 파이썬 기본 http.server로는 안 됨)

8. **실제 녹화본 테스트 ✔ 2026-09-17** — Google Meet 녹화(19분, 4명, 화면 공유 없음)로 전 과정 약 7분, 실패 단계 없음. 회의록 40항목·확인 문항 7개·만화 8컷. 드러난 문제를 반영함: 본편 밖 사담 제외 규칙, 한 번만 들린 숫자·이름에 `(확인 필요)`(퀴즈·만화가 쓰지 못하게 스크립트로 차단), 참가자 타일 화면(`participants`)을 썸네일·만화 배경에서 제외하고 타임라인을 시간 기준으로 묶음, 핵심어가 뭉개졌을 때의 2차 전사 기준, 이름표만으로 발언자를 단정하지 않는 규칙, 설명형 회의의 리포트·퀴즈 지침, 스크립트 출력 UTF-8 고정, `status.py` 오탐, `ingest.py --frames-only`.

### 서비스 형태와 비용 (2026-09-18 논의)
- 목표: **웹 서비스**, 사용자 무료·키 입력 없음, 운영자 비용 0원, 운영자 PC 꺼져 있어도 동작. (플러그인 배포는 고려하지 않음)
- 확인된 사실: Claude API·OpenAI API에는 상시 무료 등급이 없다(신규 체험 크레딧뿐). 제3자 서비스가 사용자의 Claude 구독을 쓰려면 Anthropic 사전 승인이 필요하다(Agent SDK 문서). Gemini API에는 무료 등급이 있다(입력이 제품 개선에 쓰임).
- 방향: 화면은 github.io, 글쓰기는 무료 모델 API, 무거운 전처리(오디오·키프레임 추출)는 사용자 브라우저. 실행은 Claude 에이전트 대신 **코드가 단계별로 모델을 호출**(`tools/free_model_eval/run.py`가 그 뼈대) — 스킬의 지식 문서를 프롬프트로, 스킬의 검증 스크립트를 수리 루프로 그대로 쓴다.
- **무료 모델 품질 시험 (가상 샘플 2종, `gemini-3.6-flash`)**: 리포트·퀴즈·만화 모두 검증 스크립트를 첫 시도에 통과. 편당 호출 3회, 약 2만 7천 토큰, 48초. 회의 샘플은 채점 기준 대부분 충족(최종 결정일, 담당 미정, 미결, 자료·발언 불일치 기록, 이름 없는 참석자). 놓친 것: 강의에서 슬라이드에만 있던 `s-maxage`를 강사 설명처럼 쓰고 퀴즈로까지 출제, 다툼 있는 "QA 3주"를 요약·만화에 사용, 만화가 거의 모든 컷에 화면 배경(경고는 수리 루프를 타지 않음), 항목 수가 Claude보다 적음(18~19 vs 24~34). → 보완 후보: 경고도 수리 루프에 넣기, 근거 대조표로 한 번 더 검토시키는 호출 추가.
- 무료 등급의 불안정성: 시험 당시 `gemini-3.8-flash`·`3.7-flash`·`flash-latest`는 503(수요 폭주)으로 5회 재시도 모두 실패, Pro는 무료 한도 없음(429). 오픈소스 `gemma-4-31b-it`는 같은 키로 호출되나 10분 넘게 걸림. → 모델 여러 개를 차례로 시도하는 폴백이 필수.
- 미확인: 하루 무료 한도(편수), 전사(`gemini-3.5-transcribe` 모델이 목록에 있음 — 한국어 품질·한도 미시험), 무료 등급을 공개 서비스에 써도 되는지(약관), 실제(지저분한) 전사에서의 품질 — 실제 회의 전사는 제3자 정보라 무료 등급에 보내지 않았음.

### Gemini 기준 재설계 (2026-09-18 시작)
네 개 층: ① 지식(`skills/*/references/*.md` — 모델과 무관, 그대로) ② 도구(`skills/*/scripts/*.py` — 그대로) ③ **진행기 `engine/`**(신규) ④ 서비스(백엔드 API·브라우저 전처리·화면 — 예정).
- `engine/llm.py` — Gemini 호출. 모델 목록을 차례로 시도(503·429면 기다리지 않고 다음 모델), 응답한 모델을 그 실행 동안 유지, 호출별 토큰 기록. 표준 라이브러리만 사용.
- `engine/prompts/*.md` — SKILL.md에서 에이전트용 지시(스크립트 실행 등)를 빼고 규칙·형식만 남긴 프롬프트: classify, report, review, quiz, comic. 형식 가이드·작성법은 `skills/`의 references를 그대로 붙인다.
- `engine/pipeline.py` — 유형 판별(짧은 호출) → 리포트 초안 → 검증·수리(오류와 **경고** 모두 되돌려 고치게) → **검토 호출**(근거 대조표를 주고 항목별 점검: 슬라이드에만 있는 내용, 어긋난 값의 확산, 한 번만 들린 숫자, 빠진 이유·예시) → 렌더링 → 퀴즈 → 만화 → 모아보기. Claude 에이전트가 재량으로 하던 일을 코드 단계로 고정한 것.
- 결과(가상 샘플 2종): 편당 호출 5~7회, 입력 약 3만 + 출력 4천 + 사고 1만 토큰, 85~100초. 첫 시험에서 놓쳤던 것이 메워짐 — `s-maxage`를 "슬라이드에만 있음"으로 표시하고 퀴즈에서 제외, 요약·퀴즈·만화에서 다툼 있는 "QA 3주" 제거, 만화 배경 절반만 화면, 21일 안이 기각된 이유 포함. 경고 문구에 "무엇이 어긋났는지"를 실어 주자 모델이 실제로 고침.
- 관찰: 실행 중 `gemini-3.8/3.7-flash` 503, `3.6-flash` 분당 한도 429, `3.5-flash` 503 → `2.5-flash`까지 내려가며 완주. 폴백 없이는 무료 등급으로 서비스 불가. 한 실행 안에서 모델이 바뀌면 문체가 달라질 수 있음(미평가).
- **화면 읽기 (`engine/screens.py`)** ✔ 2026-09-18 — 키프레임 8장을 한 번의 호출로 읽음(약 1만 2천 토큰). Claude가 읽은 결과와 8장 중 6장 글자 완전 일치, 나머지 2장은 화살표 기호(`->`)와 깨진 아이콘 표기 차이뿐. 검증 오류가 난 프레임만 다시 읽히는 수리 단계 포함.
- **전사 (`engine/transcribe.py`)** ✔ 2026-09-18 — 오디오를 16kHz 모노 mp3로 줄여 8분 단위로 보냄. 4분 17초 강의: 호출 1회, 약 8천 토큰, **10초, GPU 불필요**. GPU Whisper와 본문 유사도 87%(차이는 대부분 표기 — Gemini는 `ETag`, `max-age`, `304 Not Modified`로 적고 Whisper는 "이 태그", "맥스 에이지"), 문장 시작 시각 차이 대체로 1초 이내, 말실수 "하루"를 그대로 받아적음. 전사 전용 모델 `gemini-3.5-transcribe`는 빈 응답을 돌려줘 Flash로 폴백됨(시스템 지침 칸을 받지 않는 모델은 지침을 사용자 메시지에 붙이도록 호출부 보강).
- **Gemini만으로 전 과정** ✔ — 같은 강의 영상에서 화면 읽기 → 전사 → 리포트(21항목) → 퀴즈(7문항) → 만화(8컷) → 모아보기까지 완주. 합계 호출 8회, 약 7만 2천 토큰, 2분 30초. `s-maxage`는 "슬라이드에만 있음", 1시간/하루 불일치는 별도 섹션, 퀴즈에서 제외.
- 미검증: 실제 사람 목소리(여러 화자, 잡음, 말 겹침), 8분 넘는 오디오의 조각 경계, 무음 구간에서의 지어내기, 하루 무료 한도(편수).
- **무료 한도의 실체 (2026-09-18 확인)**: 429의 원인은 분당 한도가 아니라 **모델별 하루 요청 수** — 오류 응답에 `GenerateRequestsPerDayPerProjectPerModel-FreeTier 한도=20` (`gemini-3.8-flash`, `gemini-3.6-flash`). 한도는 키가 아니라 프로젝트별·모델별이고 태평양 시간 자정에 초기화된다(공식 문서). 503은 구글 쪽 혼잡(우리 한도와 무관, `3.7-flash`·`3.5-flash`에서 잦음). `2.5-flash`와 lite 계열은 같은 날 계속 응답함 — 이들의 한도 숫자는 AI Studio 한도 화면에서만 확인 가능(미확인). → 편당 호출 약 8회이므로 최신 모델 하나로는 하루 2편 남짓, 모델을 돌려 써도 모든 사용자 합쳐 하루 10편 안팎. 대응: 단순한 단계(화면 읽기·전사·유형 판별)는 한도가 넉넉한 모델로, 호출 수 줄이기, 사용자별 무료 키(한도가 사용자마다 따로 생김) 검토.
- **무료 등급 한도 (사용자 계정의 AI Studio 화면, 2026-09-18)**: Flash 계열(3.8/3.7/3.6/3.5/2.5) 각각 분당 5회·분당 25만 토큰·**하루 20회**. Flash-Lite(3.1, 3.5) 각각 분당 15회·**하루 500회**. Gemma 4 31B 분당 30회·하루 14,400회지만 **분당 토큰 1만 6천**(우리 요청 한 번이 약 2만 토큰이라 초과 → Gemma 시험이 500 오류·지연으로 실패한 원인으로 보임). 3.5 Transcribe 하루 25회·분당 토큰 1만. → Flash만 쓰면 모든 사용자 합쳐 하루 약 12편, Lite가 품질을 감당하면 하루 100편 이상. **Lite 품질 시험이 다음 관문.**
- **무료 등급 약관 (Gemini API 추가 약관 원문 확인)**: 무료 서비스에 제출한 내용과 응답은 구글 제품·머신러닝 개선에 쓰이고 **사람 검토자가 읽고 주석을 달 수 있다**(계정·키와의 연결은 끊고). "민감한 정보, 기밀 정보 또는 개인 정보를 무료 서비스에 제출하지 마세요." 유럽 경제 지역·스위스·영국 사용자에게 제공하려면 유료 서비스만 가능. 만 18세 미만을 대상으로 하거나 접근할 가능성이 있는 서비스에 쓸 수 없음. 결제 계정을 켜면 유료 서비스가 되어 개선 목적 사용에서 빠진다. → **회의 녹화(타인의 이름·목소리·업무 기밀)를 무료 등급으로 처리하는 것은 약관의 권고와 어긋난다.** 무료 서비스는 민감하지 않은 영상(공개 강의, 본인 학습 영상)으로 범위를 한정하고 고지·동의를 받아야 함. 회의 용도는 별도 경로(유료 등급 등)가 필요 — 미결정.
- **리포트·PDF**: `report.html`에서 영상 플레이어를 빼고 썸네일을 문서 안에 담아 단독으로 내려받을 수 있게 함. "PDF로 저장·인쇄" 버튼과 인쇄용 모양 추가(서버 없이 브라우저 인쇄 사용 — 인쇄 결과물은 아직 눈으로 확인 못 함). 영상과 나란히 보는 화면은 `watch.html`(파이프라인의 `make_index.py`가 생성)로 분리, 시각 클릭 시 영상 이동을 브라우저에서 확인.
- **리포트 분량 늘림**: 근거 규칙의 "분량 감각"을 고침(빠짐없이·나눠서·구체적으로, 10분 이하 영상은 1분당 4~6개). 진행기가 영상 길이에서 목표 항목 수를 계산해 넘기고, 검토 호출에 timeline도 함께 줘서 빠진 대목을 채우게 함. 회의 샘플 19→37항목, 강의 샘플 21→42항목(`gemini-2.5-flash`로 실행 — 최신 모델은 그날 한도 소진). 부작용: 진행 멘트 같은 군더더기 항목이 섞이고, 2.5-flash는 회의에서 자기소개로 확인되는 이름 대신 "화자 A/B"를 씀.
- **백엔드 API (`backend/`)** ✔ 2026-09-18 — FastAPI. 작업 접수(브라우저가 뽑은 오디오+키프레임이 기본, 개발용으로 영상 파일도) → 대기열·작업자 1개 → 단계별 진행 상태(`status.json`) → 결과 파일 제공 → 퀴즈 서버 채점·집계. 정답이 든 `quiz.json`/`quiz.html`은 내주지 않고 `quiz.public.json`만 내줌. 안전장치: 동의 필수, 하루 작업 수 상한, 영상 길이·파일 크기 제한, 추측 불가능한 작업 ID, 보관 기간 뒤 자동 삭제, CORS 설정. 키·모델·상한은 모두 환경변수(무료→유료 전환 시 `GEMINI_API_KEY`·`GEMINI_MODELS`만 교체). `Dockerfile` 포함.
  - 로컬 시험: 강의 샘플의 오디오(mp3 1MB)+키프레임 8장을 올려 **2분 만에 완료**(호출 9회, 약 8만 3천 토큰). 결과 파일 200, 정답 파일·경로 조작 404, 채점(6/8 → 다시 볼 구간 2개)·집계 동작. 이날 Flash 계열은 한도 소진·혼잡으로 대부분 막혀 일부 단계가 `gemini-3.5-flash-lite`로 넘어가 완주 — Lite가 마지막 보루로 동작함을 확인(Lite 단독 품질은 따로 채점하지 않음).
  - 아직 없는 것: 로그인·사용자별 제한, 남용 방지, 장기 보관. 무료 호스팅은 디스크가 임시라 결과물은 "만들고 바로 받아 가는" 것으로 봐야 함.
- **브라우저 전처리 (`web/js/preprocess.js`, 시험 페이지 `web/lab/preprocess.html`)** ✔ 2026-09-18 — ffmpeg.wasm 없이 브라우저 기본 기능만 사용. 키프레임: `<video>`+캔버스로 2초마다 훑으며 지각 해시 차이로 고름(서버 `ingest.py`와 같은 알고리즘; 파일을 메모리에 올리지 않음). 오디오: Web Audio로 16kHz 디코딩 → 모노 → lamejs로 32kbps MP3(`web/vendor/`에 함께 둠, LGPL-3.0).
  - 강의 샘플(4분, 3MB): 키프레임 8장 14초(서버와 같은 슬라이드 8장, 시각은 2초 격자 차이), 오디오 1.0MB 2.6초.
  - 실제 Meet 녹화(19분, 35MB — 추출만, 전송 없음): 키프레임 1장 11초(공유 화면 없는 영상이라 정상), 오디오 4.6MB 30초, JS 힙 약 240MB.
  - 브라우저 → 백엔드 전 경로 확인: 시험 페이지에서 접수(CORS를 페이지 주소로 좁힌 상태) → 3분 10초 뒤 리포트·퀴즈·만화 완료.
  - 미검증: 1시간 넘는/수백 MB 영상(오디오 디코딩이 파일 전체를 메모리에 올림 — 한계 있음), Chromium 외 브라우저(Safari·Firefox의 mp4 오디오 디코딩), 모바일.
- **화면 (`web/index.html`, `web/js/app.js`, `web/js/config.js`, `web/css/app.css`)** ✔ 2026-09-18 — 빌드 없는 정적 파일(github.io에 그대로 올림). 세 화면: 올리기(파일 선택·영상 종류·동의 체크·오늘 남은 편수) → 만드는 중(기기 안 단계 3 + 서버 단계, 4초마다 상태 확인) → 결과(왼쪽 기기 안의 영상, 오른쪽 리포트/퀴즈/만화 탭).
  - 결과 주소는 `#job=<id>` — 새로고침·공유 가능. 영상은 서버에 없으므로 다시 열면 "영상 파일 다시 선택" 안내.
  - 리포트·만화는 서버의 html을 iframe으로, 퀴즈는 화면이 직접 그리고 서버에서 채점(정답은 브라우저에 오지 않음). 시각 클릭 → 영상 이동: 퀴즈는 직접, iframe 안은 postMessage.
  - 확인: 남은 편수 표시, 동의 전 버튼 잠김, 결과 화면, 퀴즈 채점·해설·다시 볼 구간, 만화 탭, 시각 클릭 이동(퀴즈 46초, 리포트 iframe 56초). 백엔드 주소는 `config.js` 한 줄 또는 `?api=`.
  - 로컬 개발: `SERVE_WEB=1`이면 백엔드가 `web/`도 함께 내줌 → `http://127.0.0.1:8000/web/` 하나로 전체 확인.
  - 미검증: 화면과 API가 다른 주소일 때 iframe 시각 클릭(자동화 도구의 클릭이 다른 주소 iframe에 닿지 않아 손으로 확인 필요 — 같은 주소에서는 동작), 모바일 실기기, 인쇄 모양, 실패·대기열 화면의 실제 모습.
- **배포** ✔ 2026-09-18 — 코드 `github.com/JeongWoobin335/video-report-book`(공개), 화면 `jeongwoobin335.github.io/video-report-book/`(main 루트, `index.html`이 `web/`으로 넘김), 백엔드 `video-report-book.onrender.com`(Render 무료, Dockerfile, push하면 자동 재배포). 환경변수: `GEMINI_API_KEY`, `ALLOWED_ORIGINS=https://jeongwoobin335.github.io`.
  - 실제 주소에서 강의 샘플 전 과정 확인: 기기 안 추출(8장, 1.0MB) → 접수 → **4분 28초** 뒤 완료(로컬의 약 2배), 실패 단계 없음. 퀴즈 7문항 서버 채점·다시 볼 구간·영상 이동, 만화 200, 정답 파일 404.
  - 주의: Pages는 js를 10분쯤 캐시함(배포 직후 옛 화면이 보일 수 있음). Render 무료는 15분 무요청이면 잠들고 디스크가 임시 — 재배포·재시작 때 진행 중인 작업과 결과물이 사라짐.
- **심사위원용 첫 체험** ✔ 2026-09-18 — 용도가 해커톤 제출(심사위원이 2주쯤 직접 써 봄)이라 "처음 들어온 사람이 막히지 않는가"를 우선. 하루 편수 기본값 30.
  - 서버 깨우는 중 카드: 1.5초 안에 답이 없을 때만 나타남. 화면을 막지 않음 — 영상 고르기·추출은 기기 안의 일이라 먼저 진행되고, "서버로 보내기"에서만 기다림. 6초마다 서비스 특징 팁, 90%까지만 차는 막대, 깨어나면 초록으로 바뀌고 사라짐, 3분 넘으면 실패 안내 + 예시 링크.
  - "샘플 강의 영상으로 해 보기": 저장소의 가상 강의를 고른 것으로 처리(동의는 그대로 받음).
  - "완성된 예시 보기"(`#demo`, `web/demo/`): 같은 엔진으로 만든 결과를 정적으로 둠 — 서버·AI 한도를 쓰지 않고, 재배포에도 남음. 영상은 저장소의 샘플을 바로 붙임. 예시에 한해 정답을 함께 두고 브라우저에서 채점(서버 `grade.py`와 같은 규칙).
  - 로컬 확인: 예시 화면에서 리포트 시각 클릭 → 영상 32초, 퀴즈 5/7 채점과 다시 볼 구간, 만화 8컷·배경 이미지. 샘플 버튼 → 파일 선택 상태. 미검증: 실제로 잠든 Render에서의 카드 동작.
- **메인 화면 재디자인** ✔ 2026-09-18 — "너무 슴슴하다, 동의 칸 설명이 부실하다"는 지적. 만화와 같은 말투(종이색·먹선 테두리·어긋난 그림자·노란 강조)로 통일. 구성: 상단 바 → 히어로(문구 + 결과물 세 장을 겹친 그림: 리포트·만화 컷·퀴즈) → 어떻게 동작하나요(3단계) → 받아 보는 것(3가지) → 직접 해 보기(데모 안내 상자 + 올리기 카드) → 바닥글.
  - 데모 안내 상자: 무료 모델 사용 / 모든 기능은 동작 / 입력이 학습·개선에 쓰일 수 있으니 내부 회의 등 비공개 영상은 넣지 말 것 / 추후 학습에 쓰이지 않는 모델로 전환·보안 강화 예정 / 영상 파일 자체는 기기 밖으로 나가지 않음, 결과물은 24시간 뒤 삭제. 동의 문구는 "위 안내를 읽었다 + Google Gemini 무료 등급으로 전송됨"으로 구체화.
  - `#how`·`#upload` 같은 화면 안 이동에는 새로고침하지 않게 함(전에는 해시가 바뀌면 무조건 새로고침 → 고른 파일이 날아갔을 것). 휴대폰 폭 가로 넘침 없음 확인.
- **만화 그림체 교체** ✔ 2026-09-18 — 사용자가 "그림판으로 그린 것 같다"고 지적. Gemini 이미지 생성은 무료 등급 한도가 0(세 모델 모두 429, `limit: 0`)이라 제외. **Open Peeps(Pablo Stanley, CC0)** 손그림 파츠(몸·머리·얼굴)를 겹쳐 그리는 방식으로 교체 — `skills/comic-recap/assets/peeps/`(쓰는 것만 24개, 361KB), `cast.json`(캐릭터별 머리·후광 색·표정→포즈, 표정→얼굴), `characters.js`. `comic.json` 구조와 스토리보드 형식은 그대로.
  - 연출: 손그림 말풍선(짧게 외치는 대사는 노란 강조), 파스텔 배경 + 망점, 놀라는 컷에 집중선, 효과 글자(? ! …), 내레이션 띠지, 영상 화면은 천장에서 내린 스크린. 화면이 있는 컷은 가로로 넓게, 대화 컷은 둘씩 — 짝 없는 컷은 자동으로 넓힘. 휴대폰 폭에서는 스크린을 위에 크게. 글꼴은 Google Fonts(Gaegu, Jua), 못 받으면 기기 글꼴.
  - Open Peeps는 포즈와 옷이 한 파츠 → 캐릭터마다 옷이 비슷한 포즈만 묶고, 후광 색·머리·이름표로 인물을 구분. `Pointing Up`은 손이 잘려 캔버스 폭만 넓힘.
  - 표정이 곧 포즈라서 설명하는 사람이 내내 `explain`이면 같은 그림이 이어짐 → 프롬프트·storyboard에 규칙 추가, `build_comic.py`가 같은 사람·같은 표정 4컷 연속이면 경고(수리 루프가 고침). 재생성 결과 선배 표정 4종으로 분산.
  - comic.html은 쓰인 파츠만 data URI로 넣어 한 파일(약 170KB)로 열림.
- **단계별 모델 배정** ✔ 2026-09-18 — Flash는 모델당 하루 20회라 전 단계를 Flash로 돌리면 하루 10편 남짓. 기본을 Lite로 하고 판단이 필요한 단계만 Flash로(`GEMINI_STRONG_STAGES`, 기본 `report,review`; `all`이면 전 단계 목록 순서대로). 어느 쪽이든 막히면 다른 쪽으로 넘어감. 편당 Flash 2~3회 → Flash 5종 합쳐 하루 30~50편, 그 뒤는 Lite만으로 계속.
  - 강의 샘플 비교(함정: 슬라이드 "3600초 = 1시간", 강사는 "하루 동안"):
    - **Lite만**: 34초, 사고 토큰 0. 전사는 들린 대로 충실("3600이죠… 하루 동안"), 용어는 소리대로("이태그", "맥스 에이지") — 화면 글자가 있어 리포트에서는 바로잡힘. **리포트가 어긋남을 놓치고 "`max-age=3600`이면 하루 동안"이라고 사실처럼 씀** ✗. "슬라이드에만 있음" 표시도 빠짐.
    - **전사를 Flash(2.5, 사고 있음)로**: 강사의 "하루"를 "한 시간"으로 **고쳐 적음** ✗ — 전사 지침을 어기고 어긋남 자체를 지워 버림. 전사는 Lite가 더 충실했음.
    - **배포 첫 실행(모델 기록 없음)**: 어긋남은 잡았지만 강사 발언을 "86400(확인 필요)"로 **지어서 인용** ✗ → report 프롬프트에 "발언은 timeline 그대로, 없는 숫자를 계산해 넣지 않는다, 단위·기간도 본다" 추가.
    - **Lite 전사 + Flash 리포트·검토 + Lite 나머지(채택)**: 74초. 어긋남을 발언 원문 그대로 분리 ✔, 본문은 값 단정 없이 참고 표시 ✔, "슬라이드에만 있음" ✔, 퀴즈에 어긋난 값 없음 ✔, 만화가 어긋난 화면을 배경에서 뺌(경고 → 수정) ✔.
  - 상태 API의 `models`에 단계별 사용 모델이 나옴(품질 추적용).
  - 미검증: 다른 주소 iframe 안 시각 클릭(손으로 확인 필요), 잠든 서버 깨우기 안내의 실제 동작, 512MB 메모리에서 30분짜리 영상.

### 보류 (사용자 결정 2026-09-17 — 오래 걸리고 독립적이라 나중에)
- **화자 분리**: 목소리로 나누기(1단계, 모든 영상) + 증거로 이름 붙이기(2단계: 사용자 지정 > 플랫폼 자막 > 화면 이름표 집계 > 대화 속 단서). 새 스킬 `speaker-naming` 예정. 엔진은 `transcript.json`의 `speaker` 계약 뒤에 숨기므로 나중에 교체 가능(pyannote 로컬 / 서버리스 GPU / 한국어 STT API). 실서비스 비용은 실측 후 판단.
- ~~만화 그림체~~: 2026-09-18 재개해 Open Peeps로 교체함(위 "만화 그림체 교체").

### 남은 일 (전체가 돌아간 뒤)
- 실제 회의 영상(여러 화자, 화면 공유, 말 겹침)으로 검증 — 지금까지의 입력은 모두 깨끗한 가상 데이터
- 1시간 이상 영상: 키프레임 상한, 리포트의 구간 분할 방식
- general 유형(데모, 인터뷰)
- 만화 그림체
- 웹 서비스: Agent SDK 워커, 업로드·진행 상태·뷰어·퀴즈 응시 UI, 서버 채점(`quiz.public.json` + `quiz-grading`)
